# Legacy Source Snapshots

This folder contains clean source-only snapshots of the original tool locations before consolidation into gitSpecOps.

These are here as a comparison and safety reference before deleting the old working folders:

- `archive-updater/` came from `T:\Github\sb4ssman\PythonTools\ArchiveUpdater`.
- `github-org-duplicator/` came from `T:\Github\sb4ssman\github-org-duplicator`.

Intentionally excluded:

- nested `.git/` directories
- `__pycache__/`
- runtime logs and session files
- downloaded/uploaded/completed repo tracking files

The maintained package source lives in `src/git_spec_ops/`.
