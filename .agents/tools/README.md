# tools/

Agent-owned helper scripts — small, self-contained utilities used while working on this repo
(inspection, scanning, one-off automation). Distinct from the project's own tools under
`gitArchiveUpdater/` and `github-org-duplicator/`.

Output produced by these helpers belongs in [`../output/`](../output/).

_Unpopulated for now._
